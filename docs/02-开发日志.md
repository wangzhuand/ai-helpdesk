# 开发日志（给下一个 AI / 未来自己的交接文档）

> 新会话必读顺序：`01-项目规划书.md`（项目是什么）→ 本文件（进展到哪）→ `decision-log.md`（为什么这么选）。
> 每天收工更新本文件的"最新状态"。

## 协作约定（用户与 AI 的分工，必须遵守）

- **用户手动写所有业务代码**，这是第一个项目，目标是练手感和面试深度。AI 的角色是导师 + 评审：
  - 写之前：讲思路、设计、怎么下手，不写代码
  - 写之后：按 code review 标准审代码，讲隐患和改进
  - 卡住 >2 小时：AI 帮定位和讲原理，修复代码仍由用户自己敲
  - 例外：Maven/Docker 等纯配置样板、本文档类管理事务可代劳
- 项目相关一切产物写到 `D:\项目\ai客服项目\`，不写别处

## 环境现状（2026-09-03 晚更新）

| 项 | 状态 |
|---|---|
| JDK 17 / Maven 3.6.3（已配阿里云镜像 central）/ Node 24 / Git 2.54 | ✅ 就绪 |
| IDEA | ✅ 已装（`C:\Users\wangzhu\AppData\Local\Programs\IntelliJ IDEA`） |
| MySQL | ✅ 8.0 与 5.7 双服务在跑，**项目用 8.0**。用户已在 Navicat 手动执行 `V1__init.sql`，`ai_helpdesk` 库 + 全部表已建好 |
| Redis / Nacos / Tomcat / Navicat | ✅ 均有（`D:\software\`），Redis/Nacos 到对应周次再启用 |
| Docker / WSL | ✅ **已放弃本地 Docker 路线**。用户有 VirtualBox 虚拟机且虚拟机上已有 Docker → ES/RocketMQ（W4+）丢虚拟机跑。本地 `D:\software\Docker\DockerDesktopInstaller.exe`（596MB）留着备用，不用再下 WSL。注：用户可能买云服务器（W14 公网 demo 用，待定） |
| GitHub | ✅ **全通**：SSH key 已添加（用户名 `wangzhuand`），仓库 `ai-helpdesk`（Public）已建，`git remote add origin git@github.com:wangzhuand/ai-helpdesk.git` + 首次 push 已完成 |
| GLM API Key | ⏳ 用户自行注册中（bigmodel.cn），W3 才用 |

## 仓库状态

- 本地：`D:\项目\ai客服项目`，main 分支
- 远程：`git@github.com:wangzhuand/ai-helpdesk.git`，首次 push 完成 ✅
- 提交规范：feat/fix/docs/refactor/perf 前缀，小步提交；用户自己跑 git 命令练手

## 已定下的关键设计（详见 decision-log，此处防遗忘）

- 游标分页（消息表）；ES 混合检索（弃独立向量库）；RocketMQ（延迟消息刚需）；先单体后拆微服务（W10 拆 ai-service + notification-service，理由：LLM 故障隔离 + 降级转人工）
- JWT 手写拦截器（不引安全框架）；数据库 V1/V2 只增不改
- 检索/消息/延迟场景改跑虚拟机 Docker 或云服务器

## 任务看板

- [x] Day1 环境/文档/仓库/GitHub 全链路
- [x] Day2-① Navicat 执行 V1 建库（11 张表完成，用户手动）
- [ ] **Day2-②（当前）** IDEA 建 SpringBoot 工程：Spring Initializr → JDK17、SpringBoot 3.x、依赖勾 Web/Validation/Lombok/MySQL Driver（MyBatis-Plus 稍后手动加 pom）→ 阿里云镜像生效
- [ ] Day2-③ `application.yml` 数据源连 ai_helpdesk → `common/Result.java` + `GlobalExceptionHandler` → `/api/health` 冒烟 → git 提交（用户手写，AI 审）
- [ ] Day3：JWT 工具类 + 登录拦截器 + `/console/login` + 真实 bcrypt 密码（V1 里 admin 密码是占位哈希需替换）+ 装 Apifox
- [ ] Day4-7：前端骨架（AI 生成用户审）、会话/消息接口、聊天页 UI、周验收
- [ ] 挂起：GLM key（W3）；ES 装虚拟机（W4）；云服务器待定（W14）

## 面试素材备忘（随做随记）

- （待填）
