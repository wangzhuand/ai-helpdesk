# 开发日志（给下一个 AI / 未来自己的交接文档）

> **新会话必读顺序**：本文件（进展到哪 + 下一步）→ `01-项目规划书.md`（项目是什么）→ `decision-log.md`（为什么这么选）。
> 每次收工请更新下面「⭐ 当前状态与下一步」这一节。

---

## ⭐ 当前状态与下一步（新会话从这里看）

**进度：W2/W3/W4 全部完成。2026-09-16 完成全库扫描与文档对齐；2026-09-18 复核确认文档与代码一致。下一步：待办 A（代码清理 10 项，已完成 1 项、剩 9 项）→ W5（RAG 检索）。当前分支 `main`，工作区干净。**

### 开工前三条自检（血泪教训）

1. **隧道**：`curl http://localhost:9200` 必须返回 ES 的 JSON。不通就先起隧道：
   - FinalShell：启动已配的隧道（本地 127.0.0.1:9200 → 目标 127.0.0.1:9200），保持连接
   - 或 Git Bash：`ssh -L 9200:localhost:9200 ubuntu@服务器IP -N`（窗口别关）
2. **后端**：IDEA 跑 `HelpdeskApplication`，工程根目录 `D:\项目\ai客服项目\helpdesk\helpdesk`；控制台出现 `Started HelpdeskApplication` 才算好
3. **前端**：`cd /d/项目/ai客服项目/helpdesk-web && npm run dev` → 浏览器 http://localhost:5173/chat
   - ⚠️ **本机 npm 坑（2026-09-16 踩到）**：系统环境变量 `NODE_ENV=production` + `npm config omit=dev` → `npm install` **默认跳过全部 devDependencies**，`vite` / `@vitejs/plugin-vue` 根本没装上，`npm run dev` 报 `'vite' 不是内部或外部命令`。
   - 修法：装依赖用 `npm install --include=dev`；启动前先 `$env:NODE_ENV='development'`（PowerShell 里只对当前窗口生效）。
   - 判断有没有装全：`npm ls --depth=0` 只看得到 `dependencies` 就说明 dev 依赖没装。

### 待办 A：代码清理 10 项（建议走一次完整 PR 练手）

> 原版是"4 项小债"，2026-09-16 全库扫描后扩到 10 项。不是小债了，是一次正经的代码体检。
> **进度（2026-09-18 收工）**：**第 1/2/3/4/5/8/9/10 项全部完成**（第 2/3/4/5 项经 `mvn -q compile` 验证通过；第 1/8/9/10 项待跑通验证）；**第 6 项已由第 10 项消解**（`aiExecutor` 直接删除）；**第 7 项已搁置**（不做，归 W7/W11，见下）。待办 A 实质清空，下一步进 **W5-① kNN 向量检索**。

**后端（7 项，自己写）**

1. ✅ `GlobalExceptionHandler` 补两个处理器【2026-09-18 已完成】：
   - `@ExceptionHandler(MethodArgumentNotValidException.class)` → 取 `getBindingResult().getFieldErrors()` 第一条，返回 `400 + "参数校验失败：<字段> <默认消息>"`
   - `@ExceptionHandler(HttpMessageNotReadableException.class)` → `400 + "请求体缺失或格式错误"`
   - 顺序无关（Spring 按最具体的异常类型匹配，不看方法声明顺序）
   - ⚠️ 口径澄清：这两个处理器返回的是 **HTTP 200 + body 里 code=400**（`@RestControllerAdvice` 默认不给 HTTP 状态码），这是"统一信封"设计的必然结果，不是 bug。想真的返回 HTTP 400 得加 `@ResponseStatus`，但会破坏信封一致性，**决定不加**。
2. 把 `Result.java` 与 `GlobalExceptionHandler.java` 用 IDEA **Refactor → Move** 挪回 `common` 包（现在在根包，与 `BusinessException`/`JwtUtil`/`UserContext` 分裂；根包应只留启动类。规划书第 11 节的包结构参考里也写着 `common/ // Result、异常、常量、工具`，属于回归设计）
   - 影响 5 个文件的 import：`ConversationController` / `KnowledgeBaseController` / `AuthController` / `HealthController` + `GlobalExceptionHandler` 自身（Move 后它对同包的 `BusinessException` import 也变冗余）
3. `KnowledgeBaseService` 第 10/11 行重复 import 删一行
4. `application.yml` 的 `spring:` 下显式声明 `elasticsearch.uris: http://localhost:9200`（现在靠默认值，隐式易错）
5. **全量清理 13 处未使用 import**（IDE 自动补全误引的产物，编译不报错但很脏）：
   - `ConversationController` 第 6/9/13/15 行：`Visitor` / `JavaType` / `lombok.Value` / `org.antlr.runtime.Token`（原第 4 行的 `SendMessageResponse` 已随 `8594559` 移除，行号随之下移；此处已按 2026-09-18 实际文件校正）
   - `AuthService` 第 11 行 `org.apache.catalina.User`（误引 Tomcat 内部类）
   - `UserContext` 第 3 行 `javax.management.relation.Role`（误引 JMX 类）
   - `Message` 第 8 行 `java.lang.reflect.Type`
   - `Visitor` 第 7 行 `net.sf.jsqlparser.expression.DateTimeLiteralExpression`
   - `SysUser` 第 8 行 `java.math.BigInteger`
   - `MessageMapper` 第 5 行 `jakarta.validation.constraints.NotBlank`
   - `GlobalExceptionHandler` 第 3 行同包自引用 `com.example.helpdesk.Result`
   - ⚠️ **别删** `ConversationService` 第 9 行的 `Visitor`——`VisitorMapper` 和 `new Visitor()` 都在用
   - ✅ 死代码已于 2026-09-16 清除（commit `8594559`）：`ConversationService` 的两处注释块、空壳 `SendMessageResponse.java`、以及它在 `ConversationController` 里的 import
   - 遗留待定：`ConversationService` 的 `private final AiService aiService;` 现在**已无任何引用**（唯一使用点就在被删的注释里）。暂留（可能 W5/W6 会用到），若确定不用就一并删掉
6. ✅ **已由第 10 项消解（2026-09-18）：不需要改线程池了**——把 SSE 改成 `Flux.subscribe(...)`（非阻塞回调式）之后，不再需要自己开线程去跑阻塞的 `toIterable().forEach`，`aiExecutor` 已直接删除。
   - 原记录：`Executors.newCachedThreadPool()` **无上限且永不 shutdown**（全进程唯一、Spring 不管理），W11 压测必踩
   - 这正是"反应式"的价值：用回调替代阻塞等待，不占用自己的线程；自己开无界线程池反而把非阻塞的优势抵消了
   - 遗留优化（W11 再做）：`emitter.send` 目前在 Reactor 事件循环线程上做阻塞 IO，更讲究的写法是在 `subscribe` 前加 `.publishOn(Schedulers.boundedElastic())`，把回调挪到弹性线程池
7. ~~`KnowledgeBaseService.upload()` 加 `@Transactional(rollbackFor = Exception.class)`~~ → **2026-09-18 决定搁置，不做了**：
   - 原理由：现在逐块写 MySQL 无事务，中途失败留孤儿 `kb_chunk`（踩坑 17）；加事务后 ES 写失败会回滚 MySQL，反而更一致。
   - 搁置理由：① `upload()` 循环体内每块都要打一次百炼向量化 + 一次 ES 写入（**全是外部 HTTP 调用**），加事务 = 长事务 + 按"几十次网络往返"的时长占用数据库连接，W11 压测必炸；② **ES 不参与 MySQL 事务**，回滚后已写入 ES 的 chunk 依然留在 ES，孤儿数据只是从 MySQL 换到了 ES，一致性没有实质改善。
   - 归属：交给 **W7 的 MQ 异步处理**（失败重试 + 死信）+ **W11 的对账补偿**。详见 `decision-log.md` 2026-09-18 条目。

**前端（2 项，AI 直接写入 `helpdesk-web/`，2026-09-18 已写入）**

8. ✅ `api/index.js` 加请求拦截器 + 401 处理（已写入，待跑通验证）：
   - 请求拦截器：`localStorage` 有 token 就加 `Authorization: Bearer <token>`
   - ⚠️ 401 判断必须写在**响应错误分支**（`error.response?.status === 401`）。原因：后端两类错误 HTTP 行为不同——`BusinessException` 和校验异常是 **HTTP 200 + code=400**（走成功分支），而 `LoginInterceptor` 是 `setStatus(401)` + 手写 JSON（走错误分支）。写在成功分支永远不触发
9. ✅ `Chat.vue` 资源回收（已写入，待跑通验证）：`timer` / `charQueue` / `aiIndex` 提升到 `<script setup>` 顶层（现在在 `send()` 内是局部变量，`onUnmounted` 拿不到）；`onUnmounted` 里 `clearInterval(timer)` **并 `AbortController.abort()` 掐掉 fetch**。现状是中途切路由 → `setInterval` 每 15ms 跑一次永不停止 + `reader.read()` 循环变孤儿

**后端 · 2026-09-16 新发现（第 10 项）**

10. ✅ **SSE 客户端断开后不取消 LLM 调用**【2026-09-18 已完成，直接省 token】：
    - 现状：`ConversationController.sendMessage` 里是 `aiService.chatStream(recent).toIterable().forEach(token -> { ... try { emitter.send(...) } catch (Exception ignore) {} })`。`emitter.send` 失败（客户端已断开）被 `catch (Exception ignore) {}` **静默吞掉**，但 `forEach` 会继续把整个 LLM 流消费完 → **用户关掉页面/刷新后，后端仍在跟 DeepSeek 流式对话，全程照付 token**。
    - 改法：注册 `emitter.onCompletion / onTimeout / onError` 三个回调；把 `Flux` 改成 `subscribe(...)` 并持有 `Disposable`，在回调里 `disposable.dispose()` 取消上游；同时别再用 `catch (Exception ignore) {}` 静默吞异常（至少记一条 debug 日志）。
    - 与第 6 项（线程池）的关系：线程池决定"最多同时跑多少个 SSE"，第 10 项决定"每个 SSE 会不会白跑"。**第 10 项的收益更直接**。
    - **实现结果（2026-09-18）**：`toIterable().forEach` → `Flux.subscribe(规则①每来一个 token / 规则②出错 / 规则③正常结束)`；用 `AtomicReference<Disposable>` 持有订阅（lambda 只能捕获 final 局部变量，且开关要等 subscribe 返回才有值）；`onCompletion / onTimeout / onError` 三处注册 `cancelUpstream`（`getAndSet(null)` 保证幂等，且不能做成类成员变量——Controller 是单例，多人并发会互相覆盖开关）；token 转发失败时也主动取消。
    - **有意为之的取舍**：取消（cancel）不等于出错（error），`dispose()` 后规则②③都不触发，所以**半截回答不入库**，避免用户刷新回来看到一句没说完的话。
    - **验证方法（注意）**：代码里用的是 `log.debug`，Spring Boot 默认只打印 INFO 及以上，**不配日志级别是看不到的**。要么临时改 `log.info`，要么在 `application.yml` 里加 `logging.level.com.example.helpdesk: debug`。

**执行顺序（2026-09-18 定，用户已确认）**：拆成**两次提交**（直接在 `main` 上），先做零行为变更的，再做会改变行为的——出问题好回退，review 也轻松。

- **提交 1「机械清理，零行为变更」**：第 2/3/4/5 项（挪包 + 删重复 import + 清 11 处未使用 import + yml 补 `uris`）。**已于 2026-09-18 完成并通过 `mvn -q compile` 验证**。
  - 验收：编译通过、启动出现 `Started HelpdeskApplication`、登录通过、聊天流式正常。改完行为应与改前**完全一致**。
- **提交 2「行为修复」**：第 10/1/6 项（后端，按此顺序做：**SSE 断开取消 → 异常处理器 → 有界线程池**，A10 收益最直接——直接烧 token）+ 第 8/9 项（前端，AI 已写入 `helpdesk-web/`）。
  - 验收：A10 发消息后立刻关页面 → 后端日志停止输出 token；A1 不传 `title` 上传文档 → `code=400 参数校验失败：title ...`；A6 线程池有界 + 停机时 shutdown；A8 控制台接口带 token 能通；A9 流式回答中切路由无残留定时器。
- 第 7 项已搁置（见上），不在本次提交内。

**提交流程（2026-09-18 定）**：单人项目**不建功能分支、不走 PR**——直接在 `main` 上改 → `git add -A` → `git commit -m "<前缀>: ..."` → `git push`。保险措施是"每个 commit 都是可运行状态"，出问题用 `git revert <commit>` 回退。

### 待办 B：W5 详细计划（下一阶段主线）

**目标**：让系统"按意思找资料"，并让 AI 基于检索结果回答 + 给出引用来源。

> **两条已决策，不再纠结**（详见 `decision-log.md` 2026-09-15 两条）：
> ① **手写 `ElasticsearchClient` kNN + 在 Java 里手工合并 RRF**，不用 Spring AI 的 `VectorStore`——它只抽象了向量那一路、给不了 BM25，且固定 schema 与 `kb_chunk` 的 IK 分词冲突，用它等于白瞎 IK。
> ② **引用来源走 SSE 新增的 `references` 事件下发，不让模型写正文**。

**W5-① 向量检索 kNN**（新建 `RetrievalService`——一个管写、一个管读，职责分开，面试也好讲）

- 链路：用户问题 → `EmbeddingClient.embed()` → 1024 维 → ES `knn` 查 `kb_chunk` → top-k chunk
- 写法思路：`esClient.search(s -> s.index("kb_chunk").source(src -> src.filter(f -> f.excludes("embedding"))).knn(k -> k.field("embedding").queryVector(vec).k(3).numCandidates(50)), Map.class)`
  - `numCandidates` = HNSW 粗筛候选数（50），`k` = 精算后取回条数（3）。这个参数含义是面试点
  - ⚠️ `queryVector` 要 `List<Float>`，而 `embed()` 返回 `float[]`，得先装箱转换
  - ⚠️ 必须用 `_source` 排除 `embedding`，否则每次检索把 1024 个 float 从服务器拉回来
- 本步**先不接 prompt**，用日志或临时接口把召回结果打出来看
- 验收：拿一个"词面不同但语义相同"的问题（如问"怎么退款"、文档里写的是"申请退货流程"）能召回正确 chunk；问文档里没有的内容，top-3 分数明显偏低。**截图留存，W12 评测当 baseline**
- 现有数据：`kb_chunk` 里已有 2 条（1 篇文档 2 个块），可直接拿来试

**W5-② 混合检索（BM25 + 向量 + RRF）**

- 两路搜索：① `match` 查询 `content`（关键词，IK 分词）② `knn`（语义）
- 融合：RRF（Reciprocal Rank Fusion，`1/(k+rank)` 按排名倒数加权）——**在 Java 里手工合并两个结果列表**
- 为什么不用加权求和：BM25 分数无上界、余弦相似度在 [-1,1]，**量纲不同不能直接加权**；RRF 只用排名，天然免归一化
- 位置澄清：规划书第 5 节写的"混合检索 top10 → rerank 取 top3"是 **W5-②/③ 的目标态**；W5-① 只做朴素 kNN top3、不 rerank，别混
- 产出对比数据：关键词 / 向量 / 混合 三种方式的召回差异（W12 评测要用）

**W5-③ 回答带引用（走 SSE `references` 事件）**

- 后端：`AiService` 现在的 system prompt 是硬编码的"你是一个在线客服机器人…"（见 `AiService:43` 与 `AiService:60`），需要新增"带参考资料"的重载，把检索到的 chunk 拼进 prompt（要求只依据资料回答，无依据则说明并建议转人工）
- 后端：`ConversationController` 在 `done` **之前**下发 `references` 事件，payload 结构 `[{documentId, docTitle, chunkIndex, score, snippet}]`
- 前端：`Chat.vue` 加 `references` 分支 + 引用来源展示 UI
- ❌ 明确不做：让模型在正文里写「参考自《xxx》」

**W5-④ 检索测试接口**

- `GET /api/console/kb/search-test?q=xxx&mode=keyword|vector|hybrid`，返回三路结果 + 分数（控制台"检索测试"页要用；也是演示亮点）
- 口径已统一：规划书原来写的 `POST /api/v1/console/kb/retrieve-test` 已废弃，以本条为准

### 待办 C：前端适配计划（AI 出代码、用户落盘）

| 时点 | 前端要加什么 |
|---|---|
| W5 完成后 | ① 聊天页显示 AI 回答的**引用来源** ② 控制台加**检索测试页**（对比三种检索） |
| W6 完成后 | 控制台加**会话列表/接管页** + **工单管理页** → 坐席控制台骨架成型 |
| W7-9 后 | 知识库页显示文档处理进度（PENDING/PROCESSING/READY） |
| W12 后 | 评测页（跑分对比）+ 统计看板（可选） |
| W14 | 部署时统一打磨 UI |

存量前端小债：

- 新对话按钮 + 消息时间戳（W5 前端适配时顺手补）
- `api/index.js` 只有响应拦截器、没有请求拦截器 → 控制台接口不带 token（并入待办 A 第 8 项）
- `Chat.vue` 的 `setInterval` 节奏器与 `fetch` 流未回收（并入待办 A 第 9 项）
- `Login.vue` 登录成功不跳转（暂时没有控制台页，无跳转目标）→ W6
- `router/index.js` 只有 `/chat` 和 `/login`，无路由守卫、无控制台布局 → W6

### 已就绪的技术细节（W5 直接用）

- **ES 索引 `kb_chunk`**：定义已落盘到 **`docs/es/kb_chunk_mapping.json`**（2026-09-16 从服务器真实 mapping 导出）。字段：`content`(text, analyzer `ik_max_word` / search_analyzer `ik_smart`)、`doc_title`(text, **只有 analyzer `ik_max_word`，没有 search_analyzer**)、`document_id`(long)、`chunk_index`(integer)、`embedding`(`dense_vector`, dims=1024, index=true, similarity=cosine)；settings: 1 shard / 0 replica。当前库里有 **2 条数据**（W4 测试阶段上传的），W5-① 可直接拿来试
  - ⚠️ **这个索引是 W4 测试阶段人肉建的，"正式索引"的创建代码还没写**（仓库里没有建索引的代码/脚本）。见「挂起项」里的"ES 索引创建代码化"
  - ⚠️ mapping 里 `doc_title` 缺 `search_analyzer`（`content` 有）。ES 支持只更新 `search_analyzer` 而**不需要重建索引**，成本很低——是否统一待定
- **向量化**：`EmbeddingClient`（`com.example.helpdesk.client`）→ 百炼 `https://dashscope.aliyuncs.com/compatible-mode/v1/embeddings`，model `qwen3.7-text-embedding-flash`，**输出 1024 维**；key 存 `application-secret.yml`（gitignored）
- **对话模型**：DeepSeek `https://api.deepseek.com`，yml 里 model `deepseek-v4-flash`（Spring AI OpenAI starter 接入；`AiConfig` 里自建了 `@Bean ChatClient`）
- **ES 服务器**：腾讯云 2C4G / Ubuntu 26.04，Docker 容器名 `es`（8.13.4，单节点、无认证、堆 512m、`--restart=always`）；**9200 绝不能裸开公网**
- **ES Java 客户端**：Spring Boot 自动配置的 `ElasticsearchClient`（依赖 `spring-boot-starter-data-elasticsearch`）；**看不懂就翻译回 curl**：`esClient.index(...)` = `PUT /索引/_doc/{id}`
- **知识库接口**：`POST /api/console/kb/documents`（上传，需 `Authorization: Bearer <token>`）、`GET /api/console/kb/documents`（列表）；登录 `admin/admin123`
- **双写顺序**：先写 MySQL（`kb_document` 置 PROCESSING、`kb_chunk` 正本）→ 再写 ES（`es_id` = `doc-<文档id>-<块号>`）→ 成功置 READY，失败置 FAILED（一致性问题留 W7/W11）
- **分块策略**：固定长度 500 字 + 相邻重叠 50 字（W12 用评测调优）

### 后续路线图（W5 → W16）

| 周 | 内容 |
|---|---|
| W5 | RAG：kNN 向量检索 → 混合检索 BM25+kNN+RRF → 回答带引用 → 检索测试接口 |
| W6 | 工单模块（CRUD + 状态机 + 乐观锁）+ function calling（建单/查单/转人工）→ **MVP 闭环** |
| W7-8 | RocketMQ 四场景：文档异步处理 / 事件通知 / SLA 延迟升级 / 死信重试 + 幂等 |
| W9 | Redis：知识库缓存 / 抢单分布式锁 / LLM 接口限流 |
| W10 | 拆微服务：ai-service + notification-service + Nacos/Gateway + Sentinel 熔断降级 |
| W11 | 压测 + 慢 SQL 优化（before/after 数据）+ ES/MySQL 双写一致性对账 |
| W12 | 评测集 + LLM-as-judge，驱动 RAG 调优（命中率提升曲线） |
| W13 | 多步 agent（意图识别 → 检索 → 工具 → 满意度确认） |
| W14-15 | Docker Compose 部署上云 + 域名 + 公网 demo + README/架构图打磨 |
| W16 | 简历三套话术 + 查漏补缺 |

---

## 协作约定（用户与 AI 的分工，必须遵守）

- **后端业务代码由用户自己写**。AI **默认只给思路/设计（入参/出参/步骤/要用的 API/坑），不贴代码**；只有用户明确说"给我代码"时才提供（且**贴在对话里，不主动写项目文件**——用户会拒绝写入）。AI 负责评审用户代码、排错、更新文档。
- **前端代码由 AI 生成**（以"贴对话、用户自行落盘"形式交付），用户评审。
- 用户卡住 >2 小时时，AI 帮定位 + 讲原理。
- 项目相关产物一律写到 `D:\项目\ai客服项目\`。
- **接口测试优先用 Postman**（Git Bash 终端发中文有 GBK 编码坑；Postman 统一 UTF-8）；测 SSE 可用 `curl -N` 兜底。
- 服务器命令一律给**单行**（FinalShell 多行粘贴不可靠）。
- 讲解要求：先讲"为什么/是什么"，用户明确表示看不懂时要换更具体的讲法（用户曾多次因节奏太快而困惑）。

## 环境现状

| 项 | 状态 |
|---|---|
| JDK 17 / Maven 3.6.3（阿里云镜像 central）/ Node 24 / Git 2.54 | ✅ 就绪 |
| IDEA | ✅ Ultimate 旗舰版（`C:\Users\wangzhu\AppData\Local\Programs\IntelliJ IDEA`） |
| MySQL | ✅ 8.0 与 5.7 双服务在跑，项目用 8.0；`ai_helpdesk` 库 + 11 张表已建好（V1 脚本见 `docs/sql/`） |
| Redis / Nacos / Tomcat / Navicat | ✅ 均有（`D:\software\`），Redis/Nacos 到对应周次再启用 |
| 云服务器 | ✅ 腾讯云 **2核4G**（Ubuntu 26.04 resolute，FinalShell 连接，IP/密码用户自持）。Docker 29.8（阿里云 apt 源）+ 镜像加速（`mirror.ccs.tencentyun.com`）。ES 8.13.4 已跑；RocketMQ 后续也装这台。另有域名（大陆机房需 **ICP 备案**，周期 1-3 周，尚未办） |
| GitHub | ✅ SSH key 已配（用户名 `wangzhuand`），仓库 `ai-helpdesk`（Public），`origin = git@github.com:wangzhuand/ai-helpdesk.git` |
| LLM API | ✅ 对话：**DeepSeek**（已充值）；向量：**百炼** `qwen3.7-text-embedding-flash`（1024 维）。key 均存 `application-secret.yml`（gitignored），由 `spring.config.import` 引入 |

## 仓库状态

- 本地仓库根：`D:\项目\ai客服项目`（分支 `main`）
- **后端工程实际位置：`D:\项目\ai客服项目\helpdesk\helpdesk\`**（⚠️ 两层嵌套，无害但可后续压平）
- 前端工程：`D:\项目\ai客服项目\helpdesk-web\`
- 提交规范：feat/fix/docs/refactor/perf 前缀，小步提交
- **分支策略（2026-09-18 定）**：单人项目**直接在 `main` 上开发提交，不建功能分支、不走 PR**。理由是省事；代价是失去"独立分支 + 代码评审"这层保险，靠"小步提交 + 每个 commit 都可运行"兜底。若要讲面试故事，可以说"评估过 PR 流程的收益，对单人项目边际价值低，改为用小步提交 + 可运行约束保证可回滚性"

## 已定下的关键设计（详见 decision-log）

- 游标分页（消息表）；ES 混合检索（BM25 + 向量 + RRF）；RocketMQ（延迟消息刚需）；先单体后拆微服务（W10 拆 ai-service + notification-service，LLM 故障隔离 + 降级转人工）
- JWT 手写拦截器（不引安全框架）；数据库 V1/V2 只增不改
- 统一响应信封 `{code, message, data}`（code=0 成功）；`@RestControllerAdvice` 全局兜底
- 知识库：MySQL 存正本（`kb_document` / `kb_chunk`），ES 存检索副本（含 1024 维向量），靠 `es_id` 关联
- 检索层：手写 `ElasticsearchClient` kNN + Java 手工 RRF 融合，不用 Spring AI `VectorStore`（2026-09-15）
- SSE 协议：`visitor → token → references → done/error`，引用来源独立成事件下发，不让模型写正文（2026-09-15）
- 接口前缀口径：访客端 `/api/v1/conversations`；坐席控制台 `/api/console/**`（**不带 `v1`**，`/api/console/login` 豁免鉴权）

## 任务看板（历史，按时间顺序）

- [x] Day1 环境/文档/仓库/GitHub 全链路
- [x] Day2 建库（V1 11 张表）+ SpringBoot 工程搭建（手动 Maven + pom 3.5.3）+ 统一响应/全局异常/`/api/health`
- [x] Day3 JWT 登录（`POST /api/console/login`，admin/admin123）
- [x] W2 会话/消息三接口（创建会话 / 发消息 / 游标分页历史），翻页实测无重复无遗漏
- [x] Day4 登录拦截器（`LoginInterceptor` + `UserContext` + `WebConfig`，`/api/console/**` 受保护、login 豁免）
- [x] 前端骨架：Vue3 + Vite + Element Plus（聊天页 / 登录页）
- [x] W3-① Spring AI + DeepSeek 接入；W3-② 发消息带最近 20 条历史（多轮记忆）；W3-③ SSE 流式（后端事件协议 + 前端打字机节奏器 + 微信式输入框 + 自动滚底）
- [x] W4-① 云服务器 + Docker + ES 8.13.4 跑通；W4-② SSH 隧道连通；W4-③ 知识库模块（分块→向量化→双写→状态机）+ 上传/列表接口，实测通过
- [x] W4 收尾：删 3 个测试 Controller；`KbDocumentMaper`→`KbDocumentMapper`；新增 `NoResourceFoundException` 处理器
- [x] **待办 A**：代码清理 10 项（第 1-5、8-10 项已完成；第 6 项由第 10 项消解；第 7 项搁置转 W7）
- [ ] **待办 B**：W5 RAG 检索
- [x] 2026-09-16 全库扫描：后端 36 个 Java + 2 个 yml + 前端 5 个源文件 + 3 份文档；把服务器上的 ES 真实 mapping 导出落盘到 `docs/es/kb_chunk_mapping.json`（索引从"人肉建的"变成可复现）
- [ ] 挂起（现在不做，记录在案）：
  - `application-secret.yml` 会被 Maven 复制进 `target/classes` → **原样打进 jar 和 Docker 镜像**。`.gitignore` 只挡住 GitHub，挡不住 `mvn package`。当前无害（jar 只在本机跑），**W14 打镜像前必须解决**（改环境变量注入或容器外部挂载）
  - 域名 ICP 备案（大陆机房，周期 1-3 周）→ W14
  - Apifox（联调时再装）
  - 新对话按钮 + 消息时间戳
  - `Login.vue` 登录成功不跳转、`router/index.js` 无路由守卫 → W6 有 `/console` 页后一起补
  - `KbDocumentMapper` 的 javadoc 里仍写着 `KbDocumentMaper`（类名已改对，注释漏了）
  - ES 里遗留的 `demo_index`（W4 测试产物，单节点 1 副本导致 yellow）→ **2026-09-16 决定保留，不删**
  - **ES 索引创建代码化**：当前 `kb_chunk` 是 W4 **测试阶段人肉建的**，仓库里没有任何建索引的代码或脚本（定义已落盘在 `docs/es/kb_chunk_mapping.json`）。"正式索引"应该落成一个**幂等的**初始化组件（如 `EsIndexInitializer`：启动时检测索引是否存在，不存在才按 mapping 创建）→ 建议排在 W5-① 之前或同时做
    - ⚠️ **千万不要 drop 重建**：MySQL 的 `kb_chunk` 只存 `content` 和 `es_id`，**不存向量**。一旦删掉 ES 索引，所有 embedding 全部丢失，只能重新上传文档重算。所以走"存在即跳过"的幂等创建，而不是"先删后建"

## 踩坑记录（面试素材，随做随记）

1. **主类包名拼错 `hepldesk` → 接口 404**：`@SpringBootApplication` 的 `@ComponentScan` 只扫描主类所在包及子包；所有业务类必须放在主类包正下方。
2. **Spring Initializr 向导无 3.x 选项**：向导联网 start.spring.io（被墙）且默认只给最新 4.x；改用手动 Maven + pom 写死 parent `3.5.3` 走阿里云镜像。生态组件对 4.x 适配未跟上，项目锁 3.5.x。
3. **LF/CRLF 换行警告**：Windows 与 Git 换行符差异，无害；可用 `.gitattributes`（`* text=auto`）统一，暂未加。
4. **登录报"密码错误"真相**：`CommandLineRunner` 只在启动时执行一次，改完 `DataInitializer` 没彻底重启（热重载不算）→ 占位密码没被替换。教训：启动初始化类代码必须完全停进程再启。
5. **用 GET 访问 POST 接口**：浏览器地址栏发 GET，登录接口是 POST → `HttpRequestMethodNotSupportedException`。
6. **拦截器路径写 `/api/console/` 缺 `**`**：只匹配路径本身 → 子路径全裸奔；叠加 `Map.of` 不允许 null（UserContext 空时 NPE）→ 500。
7. **404/405 被兜底成 500**：`Exception.class` 大网把请求错误也包装成"系统繁忙"（已补 `NoResourceFoundException` 处理器解决 404 部分）。
8. **前端报"网络错误"= 后端 8080 没启动**：排查顺序：先 `localhost:8080/api/health`，再看 F12 Console。
9. **跨域解法**：前端 vite proxy 把 `/api` 转发到 8080，浏览器只见同源，OPTIONS 预检消失（后端放行 OPTIONS 的逻辑保留）。
10. **GLM 接入连环坑（已转 DeepSeek）**：① 未实名 → key 全 401；② Git Bash 发中文 → GBK → 400 UTF-8 错误；③ Spring AI 默认拼 `/v1/chat/completions`，GLM 只认 `/v4/` → 404，需 `completions-path: /chat/completions`（该属性靠解 jar + javap 挖出）；④ `glm-4.7-flash` 免费模型持续 1305 繁忙。
11. **MySQL 8 报 `Public Key Retrieval is not allowed`**：JDBC URL 加 `allowPublicKeyRetrieval=true`。
12. **"找不到 ChatClient Bean"**：Spring AI 自动配置只给 `ChatClient.Builder`，需自建 `@Bean ChatClient`（`AiConfig`）。
13. **SSE 前端三坑**：① 网络把多个 token 打包到达 → 前端"打字机节奏器"（队列 + 每 15ms 吐一字）平滑播放；② **Vue3 响应式坑**：对象 push 进 ref 数组后，局部变量仍指向原始对象，改它不触发渲染 → 必须改 `messages.value[下标]`；③ Enter 发送/Shift+Enter 换行用 `@keydown.enter.exact.prevent`。
14. **流式接口协议**：SSE 事件 `visitor`(回执 JSON) → `token`(逐字) → **`references`(引用来源 JSON 数组)** → `done`(落库后的完整消息 JSON) / `error`；前端用 `fetch`（非 axios）+ `ReadableStream`，帧按空行 `\n\n` 切分。
    - `references` 是 **W5 新增**的事件，在 `done` 之前发，payload `[{documentId, docTitle, chunkIndex, score, snippet}]`。
    - `done` 保持"落库后的完整消息"这一语义，**不与 references 混用**（不要为了省一个事件把引用塞进 done 的 payload）。
    - 旧协议（无 `references`）已在 2026-09-16 归档，见 decision-log 同日条目。
15. **云服务器踩坑（腾讯云 Ubuntu 26.04）**：① `get.docker.com`/`get.daocloud.io` 连不上（出站被过滤）→ 用阿里云 docker-ce apt 源（`noble` 套件在 26.04 也可用）；② **FinalShell 多行粘贴不可靠** → 一律单行命令；③ Docker Hub 直连卡死 → 配 `/etc/docker/daemon.json` 镜像加速；④ ES 需 `vm.max_map_count=262144`；⑤ **ES 启动要 1-3 分钟**，期间 curl 报 `Connection reset by peer`；⑥ ES 关了认证，**9200 绝不能裸开公网**。
16. **写 ES 报 `Connection refused` = SSH 隧道断了**（FinalShell 断开/休眠/切网都会发生）。注意向量化走公网不受影响，只有 ES 操作失败。**习惯：开工先 `curl http://localhost:9200`**。
17. **双写不一致首次现身**：先写 MySQL 后写 ES，ES 失败后 MySQL 留下孤儿 `kb_chunk`、文档状态 FAILED。W7/W11 处理（重试、清理、对账）。
18. **SLF4J 占位符**：`log.error("id=", doc.getId(), e)` 不填值，必须写 `id={}`。
19. **ES 客户端 API 心法**：看不懂 Java 客户端时，先翻译回 curl（`esClient.index(...)` = `PUT /索引/_doc/{id}`）。
20. **IK 中文分词器安装**：ES 8 的 IK 插件必须与 ES 版本完全一致；服务器出站受限 → 在本地下载 zip → FinalShell SFTP 上传 → `docker cp` 进容器 → `bin/elasticsearch-plugin install file:///tmp/ik.zip` → 重启容器。验证：`_analyze` 指定 `analyzer: ik_max_word` 看是否按词切分。
21. **ES 索引设计要点**：`content` 用 `text` + `ik_max_word`(索引侧)/`ik_smart`(查询侧)；`keyword` 用于精确匹配；单节点下 `number_of_replicas: 0` 才是 green；`dense_vector` 的 `dims` 必须等于 embedding 模型输出维度（本项目 1024）；字段新增可以、改类型/维度只能重建索引。
